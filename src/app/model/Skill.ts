export class Skill{

    id: number;
    descricao: string;
    tipo: string;

    constructor(id: number, descricao: string, tipo: string) {

        this.id = id;
        this.descricao = descricao;
        this.tipo = tipo;

    }

}